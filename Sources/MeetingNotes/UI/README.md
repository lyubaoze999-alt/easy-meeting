# 界面层 (UI)

SwiftUI 视图与菜单栏入口。仅依赖应用服务层，不直接触碰基础设施。

对应设计文档「界面层」：
- 菜单栏图标与 Popover 容器（NSStatusItem + NSPopover）
- 菜单栏面板四态：待机 / 录音 / 处理 / 完成
- 纪要库主窗口（左右分栏）
- 设置窗口（服务配置 / 模板 / 外观）
- 首次启动权限引导

视图只引用主题设计令牌（ThemeTokens），不写死色值（需求 17.4）。
所有自有文案为中文（需求 1.1）。
